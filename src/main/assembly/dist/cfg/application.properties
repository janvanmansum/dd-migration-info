daemon.http.port=20230

# User-Agent header used for http requests (such as retrieval of XSD schemas)
# when omitted the default value will be dd-migration-info/<version>
# http.agent=dd-migration-info

database.driver=org.postgresql.Driver
database.url=jdbc:postgresql://localhost:5432/dd_migration_info
database.user=dd_migration_info
database.password=changeme

dataverse.base-url=http://localhost:8080
dataverse.api-version=1
dataverse.connection-timeout-ms=10000
dataverse.read-timeout-ms=30000
dataverse.api-key=
dataverse.bucket=
